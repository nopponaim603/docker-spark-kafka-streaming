apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: dockersparkkafkastreaming-front
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/dockersparkkafkastreaming-front: "true"
  podSelector:
    matchLabels:
      io.kompose.network/dockersparkkafkastreaming-front: "true"
